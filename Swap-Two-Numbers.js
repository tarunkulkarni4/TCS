{/*


//User function Template for Java

class Solution {
    static List<Integer> get(int a, int b) {
        a = a + b;
        b = a - b;
        a = a - b;
        
        List<Integer> result = new ArrayList<>();
        result.add(a);
        result.add(b);
        return result;
    }
}


Using Bit-Manipulation:

class Solution {
    static List<Integer> swapNumbers(int a, int b) {
        a = a ^ b;
        b = a ^ b;
        a = a ^ b;
        
        List<Integer> result = new ArrayList<>();
        result.add(a);
        result.add(b);
        return result;
    }
}

    
*/}